# .agent Compatibility Shim

Source of truth for maintained agent context has moved to:

- `docs/agent-context/`

Start from:

- `docs/agent-context/README.md`
- `docs/agent-context/index.yaml`

Legacy `.agent/*.md` files are retained for compatibility and historical reference only.
