> Deprecated: This file is a compatibility copy. Source of truth moved to `docs/agent-context/` (see `docs/agent-context/quality-gates.md` and `docs/agent-context/index.yaml`).

# Agent Rules

1.  **Testing**: Always run tests after all implementations.
    -   Command: `npm run typecheck && npm run lint`
